general:
  branches:
    ignore:
      - staging
      - production
dependencies:
  pre:
    - ./scripts/setup_circleci.sh
  cache_directories:
    - ~/cache
    - ~/.cache
    - ~/.npm
    - node_modules
machine:
  node:
    version: 10.3.0
  environment:
    NODE_ENV: circleci
